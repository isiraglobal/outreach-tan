{
  "name": "outreach-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm run build --prefix frontend",
    "start": "node backend/index.js"
  }
}
